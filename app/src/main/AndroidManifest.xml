<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cc.haoduoyu.demoapp">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dropdownlistview.DropDownListViewActivity"></activity>
        <activity android:name=".span.SpanActivity"></activity>
        <activity android:name=".device.DeviceActivity"></activity>
        <activity android:name=".canvas.CanvasActivity"></activity>
        <activity android:name=".webview.WebViewActivity"></activity>
        <activity android:name=".downloadservice.DownloadActivity"></activity>
        <activity android:name=".sort.SortActivity"></activity>
        <activity android:name=".asynctask.AsyncTaskActivity"></activity>

        <service android:name=".downloadservice.DownloadService"></service>
    </application>
</manifest>
